# Cambia la estrategia de Hibernate para pruebas
spring.jpa.hibernate.ddl-auto=create-drop

# Opcional: Mostrar las consultas SQL en los logs (útil para debugging)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Opcional: Usar una base de datos H2 en memoria para pruebas (si no quieres usar PostgreSQL)
# spring.datasource.url=jdbc:h2:mem:testdb
# spring.datasource.driver-class-name=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=