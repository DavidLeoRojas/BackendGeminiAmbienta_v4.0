# ===============================
# = CONFIGURACIÓN BASE DE DATOS
# ===============================

# Render (usa variables de entorno si existen, o usa valores por defecto si ejecutas localmente)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://dpg-d42ifp0gjchc738i0v70-a.oregon-postgres.render.com:5432/gemini_db_fuxr}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:gemini_db_fuxr_user}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:exnTp7RyMUeaxax4j56caHN5Jj0f8p7d}
spring.datasource.driver-class-name=${SPRING_DATASOURCE_DRIVER_CLASS_NAME:org.postgresql.Driver}

# Dialecto para PostgreSQL
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Actualiza automáticamente las tablas (usa validate o none en producción)
spring.jpa.hibernate.ddl-auto=update

# Mostrar consultas SQL
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ===============================
# = SERVIDOR
# ===============================
server.port=${PORT:8080}

# ===============================
# = SEGURIDAD (JWT)
# ===============================
app.jwtHeader=${JWT_HEADER:Authorization}
app.jwtSecret=${JWT_SECRET:mySuperSecretKey_123456789!}
app.jwtExpirationInMs=${JWT_EXPIRATION_MS:36000000} # 10 horas

# ===============================
# = LOGGING
# ===============================
logging.level.root=INFO
logging.level.org.hibernate.SQL=INFO
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=INFO
logging.level.org.springframework.security=INFO
logging.level.com.gemini.gemini_ambiental=DEBUG

# ===============================
# = ACTUADORES
# ===============================
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=always
